
	<% provide(:title, @user.name) %>
	<div class="row">
		<aside class="col-md-4">
			<section class="user_info">
				<h1>
					<%= gravatar_for @user %>	
					<%= @user.name %>
				</h1>
			</section>
			<section  style="width: 200px; margin-top: 10px;">
				<span>Jowy Mansour est un pationné de ...r</span>
			</section>
		</aside>

		<div class="col-md-8">
		    <% if @user.microposts.any? %>
		      <h3>Microposts (<%= @user.microposts.count %>)</h3>
		      <ol class="microposts" id="my-posts">
		        <%= render @microposts %>
		      </ol>
		        <div id="infinite-scrolling" style="visibility: hidden">
		      		<%= will_paginate @microposts %> 
		      	</div>
		    <% end %>
		</div>
	</div>
